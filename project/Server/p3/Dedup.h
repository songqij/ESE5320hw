#include <iostream>
#include <vector>
#include <unordered_set>
#include <string>
#include <unordered_map>



bool deduplication(std::unordered_set<std::string>&hash_table, const std::string& hash_value);
//bool deduplication(std::unordered_map<std::string, uint32_t>& hash_table,const std::string& hash_value, uint32_t& deDup_header); 